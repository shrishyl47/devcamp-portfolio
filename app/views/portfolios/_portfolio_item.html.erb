<p><%= link_to portfolio_item.title, portfolios_show_path(portfolio_item.id) %></p>
<p><%= portfolio_item.subtitle %></p>
<p><%= portfolio_item.body %></p>
<p><%= image_tag portfolio_item.thumb_image unless portfolio_item.thumb_image.nil? %></p>
<%= link_to "Edit the portfolio item", edit_portfolio_path(portfolio_item.id) %>
<br><%= link_to "Delete the portfolio item", portfolio_path(portfolio_item.id), method: :delete, data: { confirm: 'Are you sure?' } %>